# --- Gemini (server-only; NEVER expose to the client) ---
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Model IDs — verified GA as of 2026. FAST = routine activity scoring,
# DEEP = nuanced research evaluation + profile synthesis.
GEMINI_MODEL_FAST=gemini-2.5-flash
GEMINI_MODEL_DEEP=gemini-2.5-pro

# --- Database ---
DATABASE_URL="file:./dev.db"
