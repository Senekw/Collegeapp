# Committed, NON-SECRET defaults. Read by both the Prisma CLI and Next.js.
# Do NOT put the Gemini API key here — it goes in `.env.local` (gitignored).

DATABASE_URL="file:./dev.db"

GEMINI_MODEL_FAST=gemini-2.5-flash
GEMINI_MODEL_DEEP=gemini-2.5-pro
